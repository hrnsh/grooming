<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
 <mapper namespace="kr.co.gudi.member.dao.MemberDAO">
	
	<select id="overlay" resultType="int">
	SELECT COUNT(id) FROM member WHERE id = #{param1}
	</select>

	<insert id="join" parameterType="map">
		INSERT INTO member(name,id,pw,email,phone) VALUES(#{name},#{id},#{pw},#{phone},#{email});
	</insert>
	
	<select id="logingo" resultType="String">
		SELECT id FROM member WHERE id =#{param1} AND pw = #{param2}
	</select>
	
	<select id="findID" resultType="String">
		select * from member where name=#{name} and email =#{email}
	</select>
	
	<select id="findPW" resultType="String">
		select * from member where name=#{name} and id=#{id} and email=#{email}
	</select>
	
</mapper>